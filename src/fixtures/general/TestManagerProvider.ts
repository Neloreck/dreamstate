import { createProvider } from "@/dreamstate/core/provision/createProvider";
import { TestManager } from "@/fixtures/general/TestManager";

export const TextContextManagerProvider = createProvider([TestManager]);
