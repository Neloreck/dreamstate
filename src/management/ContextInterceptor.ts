import { ContextWorker } from "./ContextWorker";


/**
 * Abstract class.
 * Class based interceptor for listening of signals and queries.
 * Middleware-like executor.
 */
export abstract class ContextInterceptor extends ContextWorker {

}
